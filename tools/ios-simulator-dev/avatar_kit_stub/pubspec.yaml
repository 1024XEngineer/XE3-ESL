name: avatar_kit
description: Explicit unsupported-device adapter for local iOS Simulator builds.
version: 1.0.0-beta.44
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
