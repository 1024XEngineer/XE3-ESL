paths:
  /v1/scenario-definitions:
    get:
      tags:
        - Preparation
      summary: List active scenario definitions
      description: |
        Returns active scenarios in stable menu order. Presentation order does
        not define a workflow or required progression.
      operationId: listScenarioDefinitions
      security: []
      responses:
        '200':
          description: Active scenarios in stable display order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScenarioDefinitionList'
              example:
                scenarios:
                  - scenario_definition_id: scn_programmer_interview
                    scenario_type: INTERVIEW
                    scenario_model: PROJECT_EXPERIENCE_DEEP_DIVE
                    name: 项目经历深挖
                    summary: 围绕一个真实项目说明个人职责、关键难点、技术取舍和结果。
                    version: 1
                    status: active
                    turn_policy_ref: interview.project_deep_dive.turn.v1
                    session_policy_ref: interview.project_deep_dive.session.v1
                  - scenario_definition_id: scn_ielts_speaking_part_2
                    scenario_type: EXAM
                    scenario_model: IELTS_SPEAKING_PART_2
                    name: IELTS Speaking Part 2
                    summary: 根据一张 Cue Card 进行连续表达，并回答考官的必要追问。
                    version: 1
                    status: active
                    turn_policy_ref: ielts.speaking_part2.turn.v1
                    session_policy_ref: ielts.speaking_part2.session.v1
                  - scenario_definition_id: scn_workplace_progress_risk_update
                    scenario_type: WORKPLACE
                    scenario_model: PROGRESS_AND_RISK_UPDATE
                    name: 项目进度与风险汇报
                    summary: 向直属领导汇报项目进展、证据、风险和需要的支持。
                    version: 1
                    status: active
                    turn_policy_ref: workplace.progress_risk_update.turn.v1
                    session_policy_ref: workplace.progress_risk_update.session.v1
                  - scenario_definition_id: scn_daily_hotel_checkin_issue
                    scenario_type: DAILY
                    scenario_model: HOTEL_CHECKIN_AND_ISSUE_HANDLING
                    name: 酒店入住与问题处理
                    summary: 在酒店前台核验预订、办理入住并处理一个房间问题。
                    version: 1
                    status: active
                    turn_policy_ref: daily.hotel_checkin_issue.turn.v1
                    session_policy_ref: daily.hotel_checkin_issue.session.v1
        '400':
          $ref: ../common/errors.yaml#/components/responses/BadRequest
        default:
          $ref: ../common/errors.yaml#/components/responses/DefaultError
  /v1/scenario-definitions/{scenario_definition_id}:
    get:
      tags:
        - Preparation
      summary: Get an active scenario definition and its configuration
      description: |
        Returns the current configuration and stable practice choices for one
        scenario. FULL_SIMULATION uses the caller's selected AI role; each
        FOCUS option is bound to exactly one role.
      operationId: getScenarioDefinition
      security: []
      parameters:
        - $ref: '#/components/parameters/ScenarioDefinitionId'
      responses:
        '200':
          description: Scenario definition, current configuration, and practice options.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScenarioDetail'
              example:
                scenario_definition:
                  scenario_definition_id: scn_programmer_interview
                  scenario_type: INTERVIEW
                  scenario_model: PROJECT_EXPERIENCE_DEEP_DIVE
                  name: 项目经历深挖
                  version: 1
                  status: active
                  turn_policy_ref: interview.project_deep_dive.turn.v1
                  session_policy_ref: interview.project_deep_dive.session.v1
                scenario_config:
                  scenario_config_id: scfg_backend_engineer
                  scenario_definition_id: scn_programmer_interview
                  config_type: INTERVIEW
                  scenario_model: PROJECT_EXPERIENCE_DEEP_DIVE
                  version: 1
                  job_title: Backend engineer
                  job_description: Build reliable APIs and explain engineering trade-offs.
                  prompt_model:
                    public_scene_brief: A technical interviewer asks you to explain one real project in depth.
                    practice_goal: Explain the context, decisions, trade-offs, and measurable outcome clearly.
                    user_role: Candidate
                    ai_role: Technical interviewer
                    persona_summary: Precise, professional, and evidence seeking.
                    focus_areas:
                      - structure
                      - engineering_tradeoffs
                      - measurable_impact
                    turn_blueprints:
                      - Ask for a concise project overview.
                      - Probe one important technical decision and its trade-off.
                      - Ask about a challenge, collaboration, and the result.
                    suggested_duration_seconds: 900
                practice_options:
                  - practice_option_id: option_full_simulation
                    scenario_definition_id: scn_programmer_interview
                    practice_option_type: FULL_SIMULATION
                    display_name: Full simulation
                    version: 1
                  - practice_option_id: option_technical_focus
                    scenario_definition_id: scn_programmer_interview
                    role_definition_id: role_technical_interviewer
                    practice_option_type: FOCUS
                    display_name: Technical depth focus
                    version: 1
                  - practice_option_id: option_hr_focus
                    scenario_definition_id: scn_programmer_interview
                    role_definition_id: role_hr_interviewer
                    practice_option_type: FOCUS
                    display_name: Recruiter and motivation focus
                    version: 1
                  - practice_option_id: option_project_manager_focus
                    scenario_definition_id: scn_programmer_interview
                    role_definition_id: role_project_manager
                    practice_option_type: FOCUS
                    display_name: Delivery and collaboration focus
                    version: 1
                  - practice_option_id: option_executive_focus
                    scenario_definition_id: scn_programmer_interview
                    role_definition_id: role_executive_interviewer
                    practice_option_type: FOCUS
                    display_name: Leadership and impact focus
                    version: 1
        '404':
          $ref: ../common/errors.yaml#/components/responses/NotFound
        default:
          $ref: ../common/errors.yaml#/components/responses/DefaultError
  /v1/scenario-definitions/{scenario_definition_id}/role-definitions:
    get:
      tags:
        - Preparation
      summary: List selectable AI roles for a scenario
      description: |
        Each role is an independently selectable AI conversation role. The
        stable response order is menu presentation only and never implies a
        mandatory workflow stage.
      operationId: listRoleDefinitions
      security: []
      parameters:
        - $ref: '#/components/parameters/ScenarioDefinitionId'
      responses:
        '200':
          description: Role definitions in stable display order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleDefinitionList'
              example:
                roles:
                  - role_definition_id: role_technical_interviewer
                    scenario_definition_id: scn_programmer_interview
                    role_type: TECHNICAL_INTERVIEWER
                    display_name: Technical depth perspective
                    responsibilities: Probe technical depth, engineering trade-offs, and decision making.
                    style: Precise and evidence seeking.
                    focus_areas:
                      - system_design
                      - project_depth
                    version: 1
                  - role_definition_id: role_hr_interviewer
                    scenario_definition_id: scn_programmer_interview
                    role_type: HR_INTERVIEWER
                    display_name: Recruiter and motivation perspective
                    responsibilities: Explore career motivation and communication clarity.
                    style: Warm and structured.
                    focus_areas:
                      - motivation
                      - communication
                    version: 1
                  - role_definition_id: role_project_manager
                    scenario_definition_id: scn_programmer_interview
                    role_type: PROJECT_MANAGER
                    display_name: Delivery and collaboration perspective
                    responsibilities: Explore delivery ownership and cross-functional collaboration.
                    style: Outcome oriented and collaborative.
                    focus_areas:
                      - delivery
                      - collaboration
                    version: 1
                  - role_definition_id: role_executive_interviewer
                    scenario_definition_id: scn_programmer_interview
                    role_type: EXECUTIVE_INTERVIEWER
                    display_name: Leadership and impact perspective
                    responsibilities: Explore leadership judgment and measurable impact for senior or management roles.
                    style: Concise, high level, and optional for advanced roles.
                    focus_areas:
                      - impact
                      - judgment
                    version: 1
        '404':
          $ref: ../common/errors.yaml#/components/responses/NotFound
        default:
          $ref: ../common/errors.yaml#/components/responses/DefaultError
  /v1/preparation-profiles:
    post:
      tags:
        - Preparation
      summary: Create or return the preparation profile for one intent
      description: |
        The authenticated actor is resolved from the Bearer Session and owns the
        created profile. The request does not accept a trusted `user_id`. A fixed
        actor may be supplied only by an explicit Mock or Test adapter. When
        supplied, `job_target_id` and `job_target_confirmation_version` bind
        the profile to one exact server-confirmed JobTarget version; the pair
        must be supplied together.
      operationId: createPreparationProfile
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePreparationProfileRequest'
            example:
              resume_ref: resume_demo_backend_v1
              job_description_ref: jd_demo_backend_v1
              background_summary: |-
                Backend engineer with three years of Go experience, responsible
                for an API reliability project and cross-team delivery.
      responses:
        '201':
          description: The preparation profile created for the current actor.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PreparationProfile'
              example:
                preparation_profile_id: prep_demo_backend
                user_id: user_demo
                resume_ref: resume_demo_backend_v1
                job_description_ref: jd_demo_backend_v1
                background_summary: |
                  Backend engineer with three years of Go experience, responsible
                  for an API reliability project and cross-team delivery.
                version: 1
                updated_at: '2026-07-23T10:00:00Z'
        '400':
          $ref: ../common/errors.yaml#/components/responses/BadRequest
        '401':
          $ref: ../common/errors.yaml#/components/responses/Unauthorized
        '409':
          $ref: ../common/errors.yaml#/components/responses/Conflict
        default:
          $ref: ../common/errors.yaml#/components/responses/DefaultError
  /v1/preparation-profiles/{preparation_profile_id}/snapshots:
    post:
      tags:
        - Preparation
      summary: Freeze a confirmed preparation profile
      operationId: createPreparationSnapshot
      parameters:
        - $ref: '#/components/parameters/PreparationProfileId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePreparationSnapshotRequest'
            example:
              source_version: 1
      responses:
        '201':
          description: Immutable preparation content for a future session snapshot.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PreparationSnapshot'
              example:
                preparation_snapshot_id: psnap_demo_backend_v1
                source_profile_id: prep_demo_backend
                source_version: 1
                resume_snapshot: Go backend engineer; API reliability project.
                job_description_snapshot: Build reliable APIs and explain engineering trade-offs.
                background_snapshot: |
                  Backend engineer with three years of Go experience, responsible
                  for an API reliability project and cross-team delivery.
                created_at: '2026-07-23T10:01:00Z'
        '400':
          $ref: ../common/errors.yaml#/components/responses/BadRequest
        '401':
          $ref: ../common/errors.yaml#/components/responses/Unauthorized
        '404':
          $ref: ../common/errors.yaml#/components/responses/NotFound
        '409':
          $ref: ../common/errors.yaml#/components/responses/Conflict
        default:
          $ref: ../common/errors.yaml#/components/responses/DefaultError
  /v1/job-targets:
    post:
      tags:
        - Preparation
      summary: Create a JobTarget draft
      description: |
        Creates actor-owned, versioned untrusted input. `job_description`
        requires inline job_description. `quick_start` requires job_title and
        omits job_description. This operation does not create a Snapshot, Plan,
        or Session.
      operationId: createJobTarget
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateJobTargetRequest'
      responses:
        '201':
          description: Created draft or the original idempotent result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobTarget'
        '400':
          $ref: ../common/errors.yaml#/components/responses/BadRequest
        '401':
          $ref: ../common/errors.yaml#/components/responses/Unauthorized
        '409':
          $ref: ../common/errors.yaml#/components/responses/Conflict
        default:
          $ref: ../common/errors.yaml#/components/responses/DefaultError
  /v1/job-targets/{job_target_id}:
    get:
      tags:
        - Preparation
      summary: Restore a JobTarget stage
      description: |
        Returns only the authenticated actor's current server-authoritative
        input, analysis, and confirmation stage. Cross-account and discarded
        targets return 404.
      operationId: getJobTarget
      parameters:
        - $ref: '#/components/parameters/JobTargetId'
      responses:
        '200':
          description: Current JobTarget recovery projection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobTarget'
        '401':
          $ref: ../common/errors.yaml#/components/responses/Unauthorized
        '404':
          $ref: ../common/errors.yaml#/components/responses/NotFound
        default:
          $ref: ../common/errors.yaml#/components/responses/DefaultError
    put:
      tags:
        - Preparation
      summary: Replace JobTarget input
      description: |
        Replaces the editable input at the expected version. A material change
        increments input_version and immediately makes prior analysis and
        confirmation inapplicable to the current projection.
      operationId: updateJobTarget
      parameters:
        - $ref: '#/components/parameters/JobTargetId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateJobTargetRequest'
      responses:
        '200':
          description: Updated draft or the original idempotent result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobTarget'
        '400':
          $ref: ../common/errors.yaml#/components/responses/BadRequest
        '401':
          $ref: ../common/errors.yaml#/components/responses/Unauthorized
        '404':
          $ref: ../common/errors.yaml#/components/responses/NotFound
        '409':
          $ref: ../common/errors.yaml#/components/responses/Conflict
        default:
          $ref: ../common/errors.yaml#/components/responses/DefaultError
  /v1/job-targets/{job_target_id}/analyses:
    post:
      tags:
        - Preparation
      summary: Start or recover JobTarget parsing
      description: |
        Claims at most one live parser attempt for the expected input version.
        A live attempt returns 202; a completed candidate returns 200. Parser
        output remains unconfirmed and cannot itself become user fact.
      operationId: analyzeJobTarget
      parameters:
        - $ref: '#/components/parameters/JobTargetId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AnalyzeJobTargetRequest'
      responses:
        '200':
          description: Parsed candidate or recovered terminal parse stage.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobTarget'
        '202':
          description: Another worker holds the current live parse lease.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobTarget'
        '400':
          $ref: ../common/errors.yaml#/components/responses/BadRequest
        '401':
          $ref: ../common/errors.yaml#/components/responses/Unauthorized
        '404':
          $ref: ../common/errors.yaml#/components/responses/NotFound
        '409':
          $ref: ../common/errors.yaml#/components/responses/Conflict
        '503':
          $ref: ../common/errors.yaml#/components/responses/DefaultError
        default:
          $ref: ../common/errors.yaml#/components/responses/DefaultError
  /v1/job-targets/{job_target_id}/confirmations:
    post:
      tags:
        - Preparation
      summary: Confirm an edited JobTarget candidate
      description: |
        Persists an explicit user-edited confirmation for the exact input and
        analysis versions. No Snapshot, Plan, or Session is created.
      operationId: confirmJobTarget
      parameters:
        - $ref: '#/components/parameters/JobTargetId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmJobTargetRequest'
      responses:
        '200':
          description: Confirmed JobTarget or original idempotent result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobTarget'
        '400':
          $ref: ../common/errors.yaml#/components/responses/BadRequest
        '401':
          $ref: ../common/errors.yaml#/components/responses/Unauthorized
        '404':
          $ref: ../common/errors.yaml#/components/responses/NotFound
        '409':
          $ref: ../common/errors.yaml#/components/responses/Conflict
        default:
          $ref: ../common/errors.yaml#/components/responses/DefaultError
  /v1/job-targets/{job_target_id}/discard:
    post:
      tags:
        - Preparation
      summary: Discard an unconfirmed JobTarget draft
      operationId: discardJobTarget
      parameters:
        - $ref: '#/components/parameters/JobTargetId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DiscardJobTargetRequest'
      responses:
        '200':
          description: Discarded target tombstone or original idempotent result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobTarget'
        '400':
          $ref: ../common/errors.yaml#/components/responses/BadRequest
        '401':
          $ref: ../common/errors.yaml#/components/responses/Unauthorized
        '404':
          $ref: ../common/errors.yaml#/components/responses/NotFound
        '409':
          $ref: ../common/errors.yaml#/components/responses/Conflict
        default:
          $ref: ../common/errors.yaml#/components/responses/DefaultError
components:
  parameters:
    ScenarioDefinitionId:
      name: scenario_definition_id
      in: path
      required: true
      schema:
        $ref: '#/components/schemas/ResourceId'
    PreparationProfileId:
      name: preparation_profile_id
      in: path
      required: true
      schema:
        $ref: '#/components/schemas/ResourceId'
    JobTargetId:
      name: job_target_id
      in: path
      required: true
      schema:
        $ref: '#/components/schemas/ResourceId'
    IdempotencyKey:
      $ref: ../common/parameters.yaml#/components/parameters/IdempotencyKey
  schemas:
    ResourceId:
      type: string
      minLength: 1
      maxLength: 128
    Version:
      type: integer
      minimum: 1
    ScenarioType:
      type: string
      enum:
        - INTERVIEW
        - EXAM
        - WORKPLACE
        - DAILY
    ScenarioModel:
      type: string
      enum:
        - PROJECT_EXPERIENCE_DEEP_DIVE
        - INTERVIEW_BASIC_DIALOGUE
        - IELTS_SPEAKING_PART_2
        - IELTS_SPEAKING_FULL_MOCK
        - EXAM_BASIC_DIALOGUE
        - PROGRESS_AND_RISK_UPDATE
        - WORKPLACE_BASIC_DIALOGUE
        - HOTEL_CHECKIN_AND_ISSUE_HANDLING
        - DAILY_BASIC_DIALOGUE
    ScenarioDefinition:
      type: object
      additionalProperties: false
      required:
        - scenario_definition_id
        - scenario_type
        - scenario_model
        - name
        - version
        - status
        - turn_policy_ref
        - session_policy_ref
      properties:
        scenario_definition_id:
          $ref: '#/components/schemas/ResourceId'
        scenario_type:
          $ref: '#/components/schemas/ScenarioType'
        scenario_model:
          $ref: '#/components/schemas/ScenarioModel'
        name:
          type: string
          minLength: 1
        version:
          $ref: '#/components/schemas/Version'
        status:
          type: string
          enum:
            - active
            - inactive
        turn_policy_ref:
          $ref: '#/components/schemas/ResourceId'
        session_policy_ref:
          $ref: '#/components/schemas/ResourceId'
    ScenarioDefinitionList:
      type: object
      additionalProperties: false
      required:
        - scenarios
      properties:
        scenarios:
          type: array
          items:
            $ref: '#/components/schemas/ScenarioDefinitionListItem'
    ScenarioDefinitionListItem:
      type: object
      additionalProperties: false
      required:
        - scenario_definition_id
        - scenario_type
        - scenario_model
        - name
        - summary
        - version
        - status
        - turn_policy_ref
        - session_policy_ref
      properties:
        scenario_definition_id:
          $ref: '#/components/schemas/ResourceId'
        scenario_type:
          $ref: '#/components/schemas/ScenarioType'
        scenario_model:
          $ref: '#/components/schemas/ScenarioModel'
        name:
          type: string
          minLength: 1
        summary:
          type: string
          minLength: 1
          maxLength: 4096
        version:
          $ref: '#/components/schemas/Version'
        status:
          type: string
          enum:
            - active
            - inactive
        turn_policy_ref:
          $ref: '#/components/schemas/ResourceId'
        session_policy_ref:
          $ref: '#/components/schemas/ResourceId'
    ScenarioConfig:
      type: object
      additionalProperties: false
      required:
        - scenario_config_id
        - scenario_definition_id
        - config_type
        - scenario_model
        - version
        - prompt_model
      properties:
        scenario_config_id:
          $ref: '#/components/schemas/ResourceId'
        scenario_definition_id:
          $ref: '#/components/schemas/ResourceId'
        config_type:
          $ref: '#/components/schemas/ScenarioType'
        scenario_model:
          $ref: '#/components/schemas/ScenarioModel'
        version:
          $ref: '#/components/schemas/Version'
        job_title:
          type: string
          minLength: 1
        job_description:
          type: string
          minLength: 1
        prompt_model:
          $ref: '#/components/schemas/ScenarioPromptModel'
    InterviewScenarioConfig:
      description: Deprecated compatibility alias for ScenarioConfig.
      allOf:
        - $ref: '#/components/schemas/ScenarioConfig'
    ScenarioPromptModel:
      type: object
      additionalProperties: false
      required:
        - public_scene_brief
        - practice_goal
        - user_role
        - ai_role
        - persona_summary
        - focus_areas
        - turn_blueprints
        - suggested_duration_seconds
      properties:
        public_scene_brief:
          type: string
          minLength: 1
        practice_goal:
          type: string
          minLength: 1
        user_role:
          type: string
          minLength: 1
        ai_role:
          type: string
          minLength: 1
        persona_summary:
          type: string
          minLength: 1
        focus_areas:
          type: array
          minItems: 1
          uniqueItems: true
          items:
            type: string
            minLength: 1
        turn_blueprints:
          type: array
          minItems: 1
          items:
            type: string
            minLength: 1
        suggested_duration_seconds:
          type: integer
          minimum: 1
    RoleDefinition:
      type: object
      description: |
        A selectable AI conversation role owned by Preparation. Its position in
        a returned list is presentation order, never a required stage.
      additionalProperties: false
      required:
        - role_definition_id
        - scenario_definition_id
        - role_type
        - display_name
        - responsibilities
        - style
        - focus_areas
        - version
      properties:
        role_definition_id:
          $ref: '#/components/schemas/ResourceId'
        scenario_definition_id:
          $ref: '#/components/schemas/ResourceId'
        role_type:
          type: string
          pattern: '^[A-Z][A-Z0-9_]*$'
          description: Stable machine type; it does not encode workflow progression.
        display_name:
          type: string
          minLength: 1
        responsibilities:
          type: string
          minLength: 1
        style:
          type: string
          minLength: 1
        focus_areas:
          type: array
          minItems: 1
          uniqueItems: true
          items:
            type: string
            minLength: 1
        voice_config_ref:
          type: string
          minLength: 1
        version:
          $ref: '#/components/schemas/Version'
    PracticeOptionDefinition:
      type: object
      description: |
        A stable practice choice for the current AI role. FULL_SIMULATION
        omits role_definition_id; FOCUS references exactly one role.
      additionalProperties: false
      required:
        - practice_option_id
        - scenario_definition_id
        - practice_option_type
        - display_name
        - version
      properties:
        practice_option_id:
          $ref: '#/components/schemas/ResourceId'
        scenario_definition_id:
          $ref: '#/components/schemas/ResourceId'
        role_definition_id:
          $ref: '#/components/schemas/ResourceId'
        practice_option_type:
          type: string
          enum:
            - FULL_SIMULATION
            - FOCUS
        display_name:
          type: string
          minLength: 1
        version:
          $ref: '#/components/schemas/Version'
    ScenarioDetail:
      type: object
      additionalProperties: false
      required:
        - scenario_definition
        - scenario_config
        - practice_options
      properties:
        scenario_definition:
          $ref: '#/components/schemas/ScenarioDefinition'
        scenario_config:
          $ref: '#/components/schemas/ScenarioConfig'
        practice_options:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/PracticeOptionDefinition'
    RoleDefinitionList:
      type: object
      additionalProperties: false
      required:
        - roles
      properties:
        roles:
          type: array
          items:
            $ref: '#/components/schemas/RoleDefinition'
    CreatePreparationProfileRequest:
      type: object
      additionalProperties: false
      required:
        - background_summary
      properties:
        resume_ref:
          type: string
          minLength: 1
          maxLength: 16384
          pattern: '^[^\s\u0000](?:[^\u0000]*[^\s\u0000])?$'
        job_description_ref:
          type: string
          minLength: 1
          maxLength: 16384
          pattern: '^[^\s\u0000](?:[^\u0000]*[^\s\u0000])?$'
        background_summary:
          type: string
          minLength: 1
          maxLength: 65536
          pattern: '^[^\s\u0000](?:[^\u0000]*[^\s\u0000])?$'
        job_target_id:
          $ref: '#/components/schemas/ResourceId'
        job_target_confirmation_version:
          $ref: '#/components/schemas/Version'
      dependentRequired:
        job_target_id:
          - job_target_confirmation_version
        job_target_confirmation_version:
          - job_target_id
    PreparationProfile:
      type: object
      additionalProperties: false
      required:
        - preparation_profile_id
        - user_id
        - background_summary
        - version
        - updated_at
      properties:
        preparation_profile_id:
          $ref: '#/components/schemas/ResourceId'
        user_id:
          $ref: '#/components/schemas/ResourceId'
        resume_ref:
          type: string
          minLength: 1
        job_description_ref:
          type: string
          minLength: 1
        background_summary:
          type: string
          minLength: 1
        job_target_id:
          $ref: '#/components/schemas/ResourceId'
        job_target_confirmation_version:
          $ref: '#/components/schemas/Version'
        version:
          $ref: '#/components/schemas/Version'
        updated_at:
          type: string
          format: date-time
      dependentRequired:
        job_target_id:
          - job_target_confirmation_version
        job_target_confirmation_version:
          - job_target_id
    CreatePreparationSnapshotRequest:
      type: object
      additionalProperties: false
      required:
        - source_version
      properties:
        source_version:
          $ref: '#/components/schemas/Version'
    PreparationSnapshot:
      type: object
      additionalProperties: false
      required:
        - preparation_snapshot_id
        - source_profile_id
        - source_version
        - background_snapshot
        - created_at
      properties:
        preparation_snapshot_id:
          $ref: '#/components/schemas/ResourceId'
        source_profile_id:
          $ref: '#/components/schemas/ResourceId'
        source_version:
          $ref: '#/components/schemas/Version'
        source_job_target_id:
          $ref: '#/components/schemas/ResourceId'
        source_job_target_confirmation_version:
          $ref: '#/components/schemas/Version'
        job_target_input_snapshot:
          $ref: '#/components/schemas/JobTargetInput'
        job_target_candidate_snapshot:
          $ref: '#/components/schemas/JobTargetCandidate'
        resume_snapshot:
          type: string
          minLength: 1
        job_description_snapshot:
          type: string
          minLength: 1
        background_snapshot:
          type: string
          minLength: 1
        created_at:
          type: string
          format: date-time
      dependentRequired:
        source_job_target_id:
          - source_job_target_confirmation_version
          - job_target_input_snapshot
          - job_target_candidate_snapshot
        source_job_target_confirmation_version:
          - source_job_target_id
          - job_target_input_snapshot
          - job_target_candidate_snapshot
        job_target_input_snapshot:
          - source_job_target_id
          - source_job_target_confirmation_version
          - job_target_candidate_snapshot
        job_target_candidate_snapshot:
          - source_job_target_id
          - source_job_target_confirmation_version
          - job_target_input_snapshot
    JobTargetSource:
      type: string
      enum:
        - job_description
        - quick_start
    JobTargetInput:
      type: object
      additionalProperties: false
      required:
        - source
      properties:
        source:
          $ref: '#/components/schemas/JobTargetSource'
        job_title:
          type: string
          minLength: 1
          maxLength: 512
        job_description:
          type: string
          minLength: 1
          maxLength: 65536
        company:
          type: string
          minLength: 1
          maxLength: 512
        seniority:
          type: string
          minLength: 1
          maxLength: 256
        candidate_background:
          type: string
          minLength: 1
          maxLength: 16384
        resume_ref:
          type: string
          minLength: 1
          maxLength: 16384
        practice_focus:
          type: string
          minLength: 1
          maxLength: 8192
      oneOf:
        - required:
            - source
            - job_description
          properties:
            source:
              const: job_description
        - required:
            - source
            - job_title
          properties:
            source:
              const: quick_start
            job_description: false
    CreateJobTargetRequest:
      $ref: '#/components/schemas/JobTargetInput'
    UpdateJobTargetRequest:
      type: object
      additionalProperties: false
      required:
        - expected_input_version
        - source
      properties:
        expected_input_version:
          $ref: '#/components/schemas/Version'
        source:
          $ref: '#/components/schemas/JobTargetSource'
        job_title:
          type: string
          minLength: 1
          maxLength: 512
        job_description:
          type: string
          minLength: 1
          maxLength: 65536
        company:
          type: string
          minLength: 1
          maxLength: 512
        seniority:
          type: string
          minLength: 1
          maxLength: 256
        candidate_background:
          type: string
          minLength: 1
          maxLength: 16384
        resume_ref:
          type: string
          minLength: 1
          maxLength: 16384
        practice_focus:
          type: string
          minLength: 1
          maxLength: 8192
      oneOf:
        - required:
            - source
            - job_description
          properties:
            source:
              const: job_description
        - required:
            - source
            - job_title
          properties:
            source:
              const: quick_start
            job_description: false
    JobTargetCatalogRecommendation:
      type: object
      additionalProperties: false
      required:
        - scenario_definition_id
        - scenario_definition_version
        - selected_role_ids
        - practice_option_id
        - practice_option_version
      properties:
        scenario_definition_id:
          $ref: '#/components/schemas/ResourceId'
        scenario_definition_version:
          $ref: '#/components/schemas/Version'
        selected_role_ids:
          type: array
          minItems: 1
          uniqueItems: true
          description: |
            INTERVIEW recommendations contain exactly one independently
            selected interviewer. A Session never combines hiring stages.
          items:
            $ref: '#/components/schemas/ResourceId'
        practice_option_id:
          $ref: '#/components/schemas/ResourceId'
        practice_option_version:
          $ref: '#/components/schemas/Version'
    JobTargetCandidate:
      type: object
      description: |
        Unconfirmed or user-confirmed structured candidate. Its compact UTF-8
        JSON encoding is limited to 65536 bytes by every server boundary.
      x-max-utf8-json-bytes: 65536
      additionalProperties: false
      required:
        - source
        - general_advice_only
        - job_title
        - seniority
        - responsibilities
        - core_skills
        - communication_focus
        - practice_goals
        - scope_notice
        - catalog_recommendation
      properties:
        source:
          $ref: '#/components/schemas/JobTargetSource'
        general_advice_only:
          type: boolean
        job_title:
          type: string
          minLength: 1
          maxLength: 512
        seniority:
          type: string
          minLength: 1
          maxLength: 256
        responsibilities:
          $ref: '#/components/schemas/JobTargetCandidateItems'
        core_skills:
          $ref: '#/components/schemas/JobTargetCandidateItems'
        communication_focus:
          $ref: '#/components/schemas/JobTargetCandidateItems'
        practice_goals:
          $ref: '#/components/schemas/JobTargetCandidateItems'
        scope_notice:
          type: string
          minLength: 1
          maxLength: 2048
        catalog_recommendation:
          $ref: '#/components/schemas/JobTargetCatalogRecommendation'
    JobTargetCandidateItems:
      type: array
      minItems: 1
      maxItems: 20
      uniqueItems: true
      items:
        type: string
        minLength: 1
        maxLength: 2048
    JobTargetAnalysis:
      type: object
      additionalProperties: false
      required:
        - input_version
        - analysis_version
        - attempt
        - status
        - started_at
      properties:
        input_version:
          $ref: '#/components/schemas/Version'
        analysis_version:
          $ref: '#/components/schemas/Version'
        attempt:
          $ref: '#/components/schemas/Version'
        status:
          type: string
          enum:
            - running
            - succeeded
            - failed
        candidate:
          $ref: '#/components/schemas/JobTargetCandidate'
        stable_error_category:
          type: string
          pattern: '^[a-z][a-z0-9_]{0,63}$'
        started_at:
          type: string
          format: date-time
        finished_at:
          type: string
          format: date-time
    JobTargetConfirmation:
      type: object
      additionalProperties: false
      required:
        - input_version
        - analysis_version
        - confirmation_version
        - candidate
        - confirmed_at
      properties:
        input_version:
          $ref: '#/components/schemas/Version'
        analysis_version:
          $ref: '#/components/schemas/Version'
        confirmation_version:
          $ref: '#/components/schemas/Version'
        candidate:
          $ref: '#/components/schemas/JobTargetCandidate'
        confirmed_at:
          type: string
          format: date-time
    JobTarget:
      type: object
      additionalProperties: false
      required:
        - job_target_id
        - user_id
        - input
        - input_version
        - stage
        - created_at
        - updated_at
      properties:
        job_target_id:
          $ref: '#/components/schemas/ResourceId'
        user_id:
          $ref: '#/components/schemas/ResourceId'
        input:
          $ref: '#/components/schemas/JobTargetInput'
        input_version:
          $ref: '#/components/schemas/Version'
        stage:
          type: string
          enum:
            - draft
            - parsing
            - analysis_failed
            - awaiting_confirmation
            - confirmed
            - discarded
        analysis:
          $ref: '#/components/schemas/JobTargetAnalysis'
        confirmation:
          $ref: '#/components/schemas/JobTargetConfirmation'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    AnalyzeJobTargetRequest:
      type: object
      additionalProperties: false
      required:
        - expected_input_version
      properties:
        expected_input_version:
          $ref: '#/components/schemas/Version'
    ConfirmJobTargetRequest:
      type: object
      additionalProperties: false
      required:
        - expected_input_version
        - expected_analysis_version
        - candidate
      properties:
        expected_input_version:
          $ref: '#/components/schemas/Version'
        expected_analysis_version:
          $ref: '#/components/schemas/Version'
        candidate:
          $ref: '#/components/schemas/JobTargetCandidate'
    DiscardJobTargetRequest:
      type: object
      additionalProperties: false
      required:
        - expected_input_version
      properties:
        expected_input_version:
          $ref: '#/components/schemas/Version'
