node_modules/
dist/
.next/
next-env.d.ts
.wrangler/
.DS_Store
*.log
.env
.env.local

# This directory contains the Sites Vite plugin source. Keep it tracked even
# though the repository root ignores Flutter build output via **/build/.
!build/
!build/**
